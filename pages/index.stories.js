import React from 'react';

import Home from './index';

export default {
  title: 'Example/Home',
  component: Home,
};

const Template = () => <Home />;

export const FirstHome = Template.bind({});
